{
  "name": "taiki-photo-capture-cloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:assets": "node scripts/build-assets.mjs",
    "cf:types": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply taiki-photo-capture --local",
    "dev": "npm run build:assets && npm run db:migrate:local && wrangler dev --local --var AUTH_MODE:development",
    "typecheck": "tsc --noEmit",
    "test:e2e": "node scripts/e2e-test.mjs",
    "check": "npm run build:assets && npm run cf:types && npm run typecheck && npm run test:e2e && wrangler deploy --dry-run --outdir .wrangler/dry-run"
  },
  "dependencies": {
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "wrangler": "^4.0.0"
  }
}
